#compdef wifi

# wifi completition function

_wifi () {
    local cmd

    if (( CURRENT > 2)); then
        # (not sure if this if-statement is required...)
    else
        _values :                   \
            "off[turn wifi off]"    \
            "on[turn wifi on]"      \
            "restart[restart wifi]"
    fi
}
